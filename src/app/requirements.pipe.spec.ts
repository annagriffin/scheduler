import { RequirementsPipe } from './requirements.pipe';

describe('RequirementsPipe', () => {
  it('create an instance', () => {
    const pipe = new RequirementsPipe();
    expect(pipe).toBeTruthy();
  });
});
