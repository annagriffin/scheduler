const { Course } = require('./course.model');
const { CourseListing } = require('./course-listing.model');
const { Requirement } = require('./requirement.model');
const { User } = require('./user.model');
module.exports = {
    Course, CourseListing, Requirement, User
}
